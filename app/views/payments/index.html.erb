
<p>
<div id="tabs-paymonth">
  <%= tabs_tag :namespace => :navigation do |tab| %>
    <%= tab.jul   'Jul', payments_path %>
    <%= tab.aug   'Aug', receipts_path %>
    <%= tab.sep   'Sep', payments_path %>
  <% end %>
</div>
</p><br /><br /><br />
<table>
  <tr>
    <th>Tax year</th>
    <th>Cat code</th>
    <th>Pay details</th>
    <th>Pay method</th>
    <th>Ref no</th>
    <th>Pay amount</th>
    <th>Gst cat</th>
    <th>Gst amount</th>
    <th>Priv use percent</th>
    <th>Priv use amount</th>
    <th>Pay month</th>
    <th>Pay day</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @payments.each do |payment| %>
  <tr>
    <td><%= payment.tax_year_id %></td>
    <td><%= payment.cat_code_id %></td>
    <td><%= payment.pay_details %></td>
    <td><%= payment.pay_method_id %></td>
    <td><%= payment.ref_no %></td>
    <td><%= payment.pay_amount %></td>
    <td><%= payment.gst_cat_id %></td>
    <td><%= payment.gst_amount %></td>
    <td><%= payment.priv_use_percent %></td>
    <td><%= payment.priv_use_amount %></td>
    <td><%= payment.pay_month %></td>
    <td><%= payment.pay_day %></td>
    <td><%= link_to 'Show', payment %></td>
    <td><%= link_to 'Edit', edit_payment_path(payment) %></td>
    <td><%= link_to 'Destroy', payment, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Payment', new_payment_path %>
